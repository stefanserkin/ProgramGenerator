<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>StagedMultiTimeMustHaveBookings</fullName>
    <active>true</active>
    <description>Staged programs that have multiple start times must have at least one staged program booking</description>
    <errorConditionFormula>AND(
  ISPICKVAL( Status__c, &quot;Staged&quot; ),
  Number_of_Staged_Program_Bookings__c == 0
)</errorConditionFormula>
    <errorDisplayField>Has_Multiple_Start_Times__c</errorDisplayField>
    <errorMessage>Staged programs that have multiple start times must have at least one staged program booking</errorMessage>
</ValidationRule>
